// Initialize Lottie Animation
var animation = lottie.loadAnimation({
    container: document.getElementById('lottie-animation'), // The container element for the animation
    renderer: 'svg', // Use SVG renderer
    loop: true, // Loop the animation
    autoplay: true, // Autoplay the animation
    path: 'Animation - 1721964467234.json' // Path to the JSON animation file
});
